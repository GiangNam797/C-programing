#include <stdio.h>

// function declaration

// the main method
int main()
{
    // Create variables
    char cName[50];
    /*    printf("Ten ban la gi? ");
        scanf("%s", cName);
        printf("\nHello %s", cName);
    */
   puts("Ten ban la gi nhi?");
   gets(cName);
   puts("\nChao ban ");
   puts(cName);
    return 0;
}

// function difinition
